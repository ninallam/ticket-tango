# Development Environment Variables
NODE_ENV=development
PORT=3001

# Database Configuration (Local Development)
# By default, SQLite is used for local development
# To use MSSQL for development, set DB_TYPE=mssql and configure the following:
# DB_TYPE=sqlite  # Options: sqlite, mssql
# DB_SERVER=localhost  # Only needed for MSSQL
# DB_NAME=tickettango  # Only needed for MSSQL
# DB_USER=sa  # Only needed for MSSQL
# DB_PASSWORD=YourPassword123!  # Only needed for MSSQL

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Client Configuration
CLIENT_URL=http://localhost:3000

# Production Environment Variables (These will be set in Azure)
# NODE_ENV=production
# DB_SERVER=your-azure-sql-server.database.windows.net
# DB_NAME=tickettango
# (No DB_USER/DB_PASSWORD needed for managed identity in production)